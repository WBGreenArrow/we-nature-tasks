export enum Status {
  PENDING = 'pending',
  IN_PROGRESS = 'in progress',
  DONE = 'done',
}

export enum StatusList {
  'pending' = 'pendingTasks',
  'in progress' = 'inProgressTasks',
  'done' = 'doneTasks',
}
